۱. ارسال واقعی ایمیل/پیامک
توی accounts/views.py جاهایی مثل send_verification_view و password_reset_request_view فقط کامنت # In production, send SMS/email here دارن — یعنی کد تایید و توکن بازیابی رمز اصلاً برای کاربر ارسال نمی‌شه. این یعنی فعلاً تایید شماره/ایمیل و فراموشی رمز عملاً کار نمی‌کنن. باید یه سرویس مثل Kavenegar (پیامک) یا SMTP/SendGrid (ایمیل) وصل کنید.


EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
# EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'    # برای چاپ در کنسول
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True #برای ارتباطات امن
EMAIL_HOST_USER = 'mkhmdyamyr8@gmail.com'
EMAIL_HOST_PASSWORD = 'xrlp ncjo aplz fiov'
DEFAULT_FROM_EMAIL = 'mkhmdyamyr8@gmail.com'
