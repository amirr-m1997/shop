IMPORTANT WORKFLOW RULES

- ALL agent output must be written in ENGLISH.
- Do NOT create any Git commit.
- Do NOT commit automatically.
- Do NOT apply migrations.
- Do NOT modify database data.
- Do NOT remove model registrations or admin functionality unless they exist only for Unfold/custom admin.
- Preserve all existing Django Admin model registrations and permissions where possible.

TASK — RESTORE DJANGO ADMIN TO THE DEFAULT DJANGO ADMIN UI

The project currently uses django-unfold and custom Admin styling.

I want the Admin panel restored to the standard/default Django Admin interface.

IMPORTANT:
This means:

KEEP:
- Django admin
- all currently registered models
- model CRUD functionality
- existing permissions
- existing custom admin actions that are business-related
- Loyalty management
- Support/Fashion Stylist management
- Orders
- Products
- Cart
- Users
- Chat-related admin
- any other legitimate model registrations

REMOVE/STOP USING:
- django-unfold
- Unfold-specific styling
- Unfold-specific templates
- Unfold-specific configuration
- Unfold-only CSS/JS
- custom AdminSite implementations that exist only for visual customization

The final Admin should look and behave like normal Django Admin 4.2.x.

## 1. READ-ONLY AUDIT FIRST

Inspect:

- settings.py
- INSTALLED_APPS
- MIDDLEWARE if relevant
- ROOT_URLCONF/admin URLs
- all admin.py files
- custom AdminSite classes
- admin templates
- static admin overrides
- Unfold configuration
- requirements.txt
- any package imports from unfold

Determine exactly how the current Admin is customized.

Do NOT modify anything during the audit.

## 2. IDENTIFY UNFOLD DEPENDENCIES

Find every usage of:

- unfold
- unfold.admin
- unfold.contrib
- UNFOLD
- Unfold-specific AdminSite
- Unfold template overrides

Determine which files are safe to remove/change.

IMPORTANT:
Do not remove any model registration merely because its admin.py imports Unfold.
Convert it to standard Django admin syntax where necessary.

## 3. RESTORE DEFAULT DJANGO ADMIN

The final configuration should use:

django.contrib.admin

and the standard Django AdminSite.

Remove Unfold from active application configuration.

If a custom AdminSite exists only to customize the visual appearance:
- replace it with the standard Django admin.site
- preserve registered models

Do not change model permissions/business actions unnecessarily.

## 4. TEMPLATES / STATIC

Remove only custom Admin templates/CSS/JS that are specifically overriding Django Admin appearance.

Do not delete templates/static files that are used elsewhere in the application.

The final Admin should load Django's built-in:
- base_site.html
- base.html
- login.html
- change_list.html
- change_form.html

unless there is a legitimate functional override that should remain.

## 5. REQUIREMENTS

Inspect requirements.txt.

If `django-unfold==0.67.0` is only used for Admin appearance and no longer needed elsewhere:

- remove the Unfold dependency from requirements.txt

Do NOT change Django version.

The project currently targets Django 4.2.30.

Do NOT upgrade Django.

## 6. ADMIN FUNCTIONALITY PRESERVATION

After reverting the UI, verify that these remain available in Django Admin:

- Users
- Groups
- Products
- Orders
- Cart
- Loyalty
- ReferralAttribution
- LoyaltyTransaction
- LoyaltyRedemption
- SupportConversation
- support/fashion stylist staff management
- any existing application-specific models

Do not remove registrations.

## 7. TESTING

Use the project's intended environment:

.venv\Scripts\python.exe

Run:

- python manage.py check
- python manage.py makemigrations --check --dry-run
- relevant Django Admin tests if present
- relevant account/admin tests if present

Verify:
- no Unfold import errors
- no template errors
- no admin URL errors
- standard `/admin/` loads through `django.contrib.admin.site`

Do NOT apply migrations.

## 8. IMPORTANT

Do NOT modify:
- private Chat
- Style Rooms
- Support business logic
- Loyalty logic
- Inventory
- Checkout
- Payments

Only restore the Admin UI/architecture to standard Django Admin.

Do NOT create a Git commit.

At the end provide ONLY:

### Summary
- Important work completed:
- Unfold usage removed:
- AdminSite:
- Model registrations preserved:
- Templates/static changes:
- Requirements change:
- Django checks:
- Migration checks:
- Admin verification:
- Files changed:
- Issues found:
- Current status:
- Next step: